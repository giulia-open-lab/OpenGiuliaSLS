import os

ROOT_DIR = os.path.dirname(os.path.abspath(__file__))
"""Root directory of the project. This is, where are the source code files are stored (giulia)"""

PROJ_DIR = os.path.join(ROOT_DIR, '..')
"""The directory for all the files of the project, aka the repository"""
